# Copy to .env.local and fill in, or run `vercel env pull .env.local`
# to have Vercel write the real values for you.
#
# Neon gives you this on the project dashboard under "Connection string".
# Use the POOLED connection string for serverless functions.
DATABASE_URL="postgresql://USER:PASSWORD@HOST-pooler.REGION.aws.neon.tech/DBNAME?sslmode=require"
